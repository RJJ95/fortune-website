import styles from "./register-button.module.css";

const RegisterButton = ({ RegisterButton, onClick }) => (
  <RegisterButton className={styles.registerButton} onClick={onClick} />
);

export default RegisterButton;
